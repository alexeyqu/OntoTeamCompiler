#pragma once

namespace enums{
	enum TArithmeticOperation { ADD, SUB, MUL, DIV, MOD };
	enum TBooleanOperation { AND, OR, LESS, GREATER };
	enum TType { INTEGER, INTEGERARRAY, BOOLEAN, STRING, CLASS };
}