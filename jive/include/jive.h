#pragma once

namespace enums{
	enum TArithmeticOperation { ADD, SUB, MUL, DIV, MOD };
	enum TBooleanOperation { AND, OR, LESS, GREATER };
	enum TType { INTEGER, INTEGERARRAY, BOOLEAN, STRING, STRINGARRAY, CLASS, IDENTIFIER, FAKETYPE, USERTYPE, VOID };
}